# Daily specials

Underlined heading
------------------

Plain text stays warm white; **strong**, *emphasis*, and ***both*** use style.

> A quiet quote with an `inline code` span.

- Soup and bread
1. [Menu](https://example.com/menu "Daily menu")

`const inline = 3`

```typescript
const price = 12.5;
function label(item: string) { return `${item}\n`; }
```

---
